' NOTE: You can use the "Rename" command on the context menu to change the class name "regAuth" in code, svc and config file together.
' NOTE: In order to launch WCF Test Client for testing this service, please select regAuth.svc or regAuth.svc.vb at the Solution Explorer and start debugging.
Public Class regAuth
    Implements IregAuth

    Public Sub DoWork() Implements IregAuth.DoWork
    End Sub

End Class
